let usernames = ["Zainrjanjua", "Golu", "Zainu", "admin", "Motu"];

usernames.forEach((item) => {
  item === "admin"
    ? console.log(` Hello admin, would you like to see a status report?`)
    : console.log(`Hello ${item}, thank you for logging in again`);
});